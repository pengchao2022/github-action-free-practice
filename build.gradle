plugins {
    id 'java'
    id 'application'
}

application {
    mainClass = 'Hello'
}

repositories {
    mavenCentral()
}

// 这个配置确保 JAR 文件包含正确的 MANIFEST.MF
jar {
    manifest {
        attributes(
            'Main-Class': application.mainClass
        )
    }
}
